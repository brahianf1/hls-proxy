# .env.example
# Este archivo sirve como plantilla para las variables de entorno necesarias.
# No guardes secretos aquí. Copia este archivo a .env.development para desarrollo local.

# --- Server Configuration ---
PORT=8000
APP_PUBLIC_URL=http://localhost:8000
TZ='America/Argentina/Buenos_Aires'

# --- Supabase ---
# URL y clave de servicio de tu proyecto Supabase.
# La SERVICE_KEY es un secreto y debe ser manejada con cuidado.
SUPABASE_URL="https://your-project-url.supabase.co"
SUPABASE_SERVICE_KEY="your-supabase-service-role-key"

# --- Security ---
# Clave de API para proteger los endpoints de la aplicación.
API_KEY="your-super-secret-proxy-api-key"

# --- CORS ---
# Lista de orígenes permitidos separados por comas.
# Para desarrollo local, puedes usar "http://localhost:3000" si tienes un frontend.
# Para producción, especifica los dominios de tu aplicación.
CORS_ORIGINS="http://localhost:8000"

# --- Upstream Resolver Service ---
# URL base y clave de API para el servicio externo que resuelve los streams.
RESOLVER_BASE_URL="http://localhost:8080"
RESOLVER_API_KEY="your-super-secret-resolver-api-key"

# --- Session & Upstream Configuration ---
# Tiempo de vida de la sesión en segundos (900s = 15 minutos).
SESSION_TTL_S=900
ACCEPT_LANGUAGE="en-US,en;q=0.9"